import React from 'react';

const UserList = () => {
    return (
        <div className="user-list">
            <h2>User List</h2>
            {/* Display list of users */}
        </div>
    );
};

export default UserList;
